# Project-specific ProGuard/R8 rules.
# Minification is disabled for the FYP (isMinifyEnabled = false), so these are
# placeholders for when release shrinking is enabled later. See docs/architecture.md.

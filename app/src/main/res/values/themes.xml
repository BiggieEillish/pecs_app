<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Base window theme only. All UI is drawn by Compose (MaterialTheme), so this just
      provides a no-action-bar window. enableEdgeToEdge() handles the system bars.
    -->
    <style name="Theme.PicExchange" parent="@android:style/Theme.Material.Light.NoActionBar" />
</resources>
